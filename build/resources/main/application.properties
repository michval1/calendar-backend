spring.datasource.url=jdbc:mysql://prosnansky.gotdns.ch:3306/super_duper_db_server
spring.datasource.username=ela
spring.datasource.password=1a2B_987_xYz
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true
# Add this to your application.properties
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

# Firebase config
firebase.credentials.path=/path/to/firebase-credentials.json

# Server configuration
server.port=8080
server.address=0.0.0.0

# Logging to see what's happening
logging.level.com.calendar=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.web.cors=DEBUG

# API prefix
server.servlet.context-path=/api/v1